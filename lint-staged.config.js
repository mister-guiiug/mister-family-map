// Convention famille (eslint --fix + prettier --write + tsc -b). Nécessite
// `lint-staged` + husky — cf. README, section « Hooks git ».
export { default } from '@mister-guiiug/dev-wpa-config/lint-staged';
