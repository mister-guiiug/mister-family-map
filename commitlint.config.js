// Convention famille (Conventional Commits). Nécessite `@commitlint/cli` +
// `@commitlint/config-conventional` (à installer avec husky — cf. README,
// section « Hooks git »).
export { default } from '@mister-guiiug/dev-pwa-config/commitlint';
